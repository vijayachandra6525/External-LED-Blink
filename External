const int LED = D7;
void setup()
{
	pinMode (LED, OUTPUT); //LED_BUILTIN Pin as an output
}
void loop()
{
   digitalWrite(LED, LOW); //Turn the LED on
   delay(1000);                    //Wait for a second
   digitalWrite(LED, HIGH); // TURN THE LED OFF
   delay(2000);			   //Wait for two seconds
}
